```
	class DButil:  
	    conn = None  
	  
	    @classmethod  
	    def __get_con(cls):  
	        if cls.conn is None:  
	            cls.conn = pymysql.connect(host='127.0.0.1', port=3306, user='root', password='0108', database='test_db')  
	        return cls.conn  
	  
	    @classmethod  
	    def __close_con(cls):  
	        if cls.conn is not None:  
	            cls.conn.close()  
	            cls.conn = None  
	  
	    @classmethod  
	    def select_one(cls, sql):  
	        cursor = None  
	        res = None  
	        try:  
	            cls.conn = cls.__get_con()  
	            cursor = cls.conn.cursor()  
	            cursor.execute(sql)  
	            res = cursor.fetchone()  
	        except Exception as e:  
	            print(str(e))  
	        finally:  
	            if cursor is not None:
	                cursor.close()
	            cls.__close_con()  
	            return res  
	  
	    @classmethod  
	    def uid_db(cls, sql):  
	        cursor = None  
	        res = None  
	        try:  
	            cls.conn = cls.__get_con()  
	            cursor = cls.conn.cursor()  
	            cursor.execute(sql)  
	            cls.conn.commit()  
	        except Exception as e:  
	            cls.conn.rollback()  
	            print(str(e))  
	        finally:  
	            if cursor is not None:
	                cursor.close()
	            cls.__close_con()  
	            return res
	```
	---------------------------------------------------------------
	```
	# db_utils.py
	import pymysql
	from pymysql.cursors import DictCursor
	import threading
	from contextlib import contextmanager
	import json
	import logging
	from datetime import datetime
	from typing import Any, Optional, List, Dict, Union, Tuple
	
	# 配置日志
	logging.basicConfig(level=logging.INFO)
	logger = logging.getLogger(__name__)
	
	class DatabaseConfig:
	    """数据库配置类"""
	    def __init__(self, config_file: str = None, **kwargs):
	        self.config = {
	            'host': 'localhost',
	            'user': 'root',
	            'password': '',
	            'database': 'test',
	            'port': 3306,
	            'charset': 'utf8mb4',
	            'cursorclass': DictCursor,
	            'autocommit': False,
	            'connect_timeout': 10,
	            'read_timeout': 30,
	            'write_timeout': 30,
	        }
	        
	        # 从配置文件加载（如果提供）
	        if config_file:
	            self._load_from_file(config_file)
	        
	        # 覆盖配置
	        self.config.update(kwargs)
	    
	    def _load_from_file(self, config_file: str):
	        """从JSON文件加载配置"""
	        try:
	            with open(config_file, 'r', encoding='utf-8') as f:
	                file_config = json.load(f)
	                self.config.update(file_config)
	        except Exception as e:
	            logger.warning(f"无法加载配置文件 {config_file}: {e}")
	
	class DatabaseConnectionPool:
	    """简单的数据库连接池（生产环境建议使用DBUtils或SQLAlchemy池）"""
	    def __init__(self, config: DatabaseConfig, pool_size: int = 5):
	        self.config = config
	        self.pool_size = pool_size
	        self._pool = []
	        self._lock = threading.Lock()
	        self._current_connections = 0
	    
	    def get_connection(self):
	        """从池中获取连接"""
	        with self._lock:
	            if self._pool:
	                return self._pool.pop()
	            
	            if self._current_connections < self.pool_size:
	                self._current_connections += 1
	                try:
	                    return pymysql.connect(**self.config.config)
	                except Exception as e:
	                    self._current_connections -= 1
	                    raise e
	            
	            raise Exception("连接池已满，请稍后重试")
	    
	    def return_connection(self, conn):
	        """归还连接到池"""
	        with self._lock:
	            if len(self._pool) < self.pool_size:
	                self._pool.append(conn)
	            else:
	                conn.close()
	                self._current_connections -= 1
	
	class DBUtils:
	    """主数据库工具类"""
	    
	    _instance = None
	    _lock = threading.Lock()
	    
	    def __new__(cls, *args, **kwargs):
	        """单例模式"""
	        if not cls._instance:
	            with cls._lock:
	                if not cls._instance:
	                    cls._instance = super().__new__(cls)
	        return cls._instance
	    
	    def __init__(self, config: DatabaseConfig = None, use_pool: bool = False):
	        if not hasattr(self, 'initialized'):
	            self.config = config or DatabaseConfig()
	            self.use_pool = use_pool
	            self._connection_pool = None
	            
	            if use_pool:
	                self._connection_pool = DatabaseConnectionPool(self.config)
	            
	            self.initialized = True
	            logger.info("数据库工具类初始化完成")
	    
	    def _get_connection(self):
	        """获取数据库连接"""
	        if self.use_pool and self._connection_pool:
	            return self._connection_pool.get_connection()
	        
	        return pymysql.connect(**self.config.config)
	    
	    def _close_connection(self, conn):
	        """关闭数据库连接"""
	        if self.use_pool and self._connection_pool:
	            self._connection_pool.return_connection(conn)
	        else:
	            conn.close()
	    
	    @contextmanager
	    def get_cursor(self, connection=None):
	        """
	        上下文管理器：自动管理游标
	        
	        用法：
	        with db.get_cursor() as cursor:
	            cursor.execute("SELECT ...")
	        """
	        conn = connection or self._get_connection()
	        cursor = None
	        try:
	            cursor = conn.cursor()
	            yield cursor
	            if not connection:  # 如果不是外部传入的连接，自动提交
	                conn.commit()
	        except Exception as e:
	            if not connection:
	                conn.rollback()
	            logger.error(f"数据库操作失败: {e}")
	            raise e
	        finally:
	            if cursor:
	                cursor.close()
	            if not connection:  # 如果不是外部传入的连接，自动关闭
	                self._close_connection(conn)
	    
	    @contextmanager
	    def transaction(self):
	        """
	        事务上下文管理器
	        
	        用法：
	        with db.transaction() as conn:
	            with conn.cursor() as cursor:
	                cursor.execute("UPDATE ...")
	                cursor.execute("INSERT ...")
	        """
	        conn = self._get_connection()
	        try:
	            # PyMySQL 默认 autocommit=False，无需显式 begin()，直接执行 SQL 即处于事务中
	            yield conn
	            conn.commit()
	            logger.info("事务提交成功")
	        except Exception as e:
	            conn.rollback()
	            logger.error(f"事务回滚: {e}")
	            raise e
	        finally:
	            self._close_connection(conn)
	    
	    # ============ 基础CRUD操作 ============
	    
	    def query_one(self, sql: str, params: tuple = None) -> Optional[Dict]:
	        """查询单条记录"""
	        with self.get_cursor() as cursor:
	            cursor.execute(sql, params or ())
	            return cursor.fetchone()
	    
	    def query_all(self, sql: str, params: tuple = None) -> List[Dict]:
	        """查询多条记录"""
	        with self.get_cursor() as cursor:
	            cursor.execute(sql, params or ())
	            return cursor.fetchall()
	    
	    def query_page(self, sql: str, params: tuple = None, 
	                   page: int = 1, page_size: int = 20) -> Dict:
	        """
	        分页查询
	        
	        返回: {
	            'data': [...],      # 当前页数据
	            'total': 100,       # 总记录数
	            'page': 1,          # 当前页码
	            'page_size': 20,    # 每页大小
	            'total_page': 5     # 总页数
	        }
	        """
	        # 查询总数（从SQL中提取COUNT语句）
	        count_sql = f"SELECT COUNT(*) as total FROM ({sql}) as t"
	        total_result = self.query_one(count_sql, params)
	        total = total_result['total'] if total_result else 0
	        
	        # 分页查询数据
	        offset = (page - 1) * page_size
	        paged_sql = f"{sql} LIMIT {offset}, {page_size}"
	        data = self.query_all(paged_sql, params)
	        
	        total_page = (total + page_size - 1) // page_size
	        
	        return {
	            'data': data,
	            'total': total,
	            'page': page,
	            'page_size': page_size,
	            'total_page': total_page
	        }
	    
	    def execute(self, sql: str, params: tuple = None) -> int:
	        """执行更新操作（INSERT/UPDATE/DELETE）"""
	        with self.get_cursor() as cursor:
	            cursor.execute(sql, params or ())
	            return cursor.rowcount
	    
	    def executemany(self, sql: str, params_list: list) -> int:
	        """批量执行"""
	        with self.get_cursor() as cursor:
	            cursor.executemany(sql, params_list)
	            return cursor.rowcount
	    
	    def insert(self, table: str, data: Dict) -> int:
	        """插入单条记录，返回自增ID"""
	        columns = ', '.join(data.keys())
	        placeholders = ', '.join(['%s'] * len(data))
	        sql = f"INSERT INTO {table} ({columns}) VALUES ({placeholders})"
	        
	        with self.get_cursor() as cursor:
	            cursor.execute(sql, tuple(data.values()))
	            return cursor.lastrowid
	    
	    def batch_insert(self, table: str, data_list: List[Dict]) -> int:
	        """批量插入"""
	        if not data_list:
	            return 0
	        
	        columns = ', '.join(data_list[0].keys())
	        placeholders = ', '.join(['%s'] * len(data_list[0]))
	        sql = f"INSERT INTO {table} ({columns}) VALUES ({placeholders})"
	        
	        values = [tuple(item.values()) for item in data_list]
	        return self.executemany(sql, values)
	    
	    def update(self, table: str, data: Dict, where: str, where_params: tuple = None) -> int:
	        """更新记录"""
	        set_clause = ', '.join([f"{k} = %s" for k in data.keys()])
	        sql = f"UPDATE {table} SET {set_clause} WHERE {where}"
	        
	        params = tuple(data.values()) + (where_params or ())
	        return self.execute(sql, params)
	    
	    def delete(self, table: str, where: str, where_params: tuple = None) -> int:
	        """删除记录"""
	        sql = f"DELETE FROM {table} WHERE {where}"
	        return self.execute(sql, where_params)
	    
	    # ============ 高级功能 ============
	    
	    def call_procedure(self, proc_name: str, args: tuple = None):
	        """调用存储过程"""
	        with self.get_cursor() as cursor:
	            cursor.callproc(proc_name, args or ())
	            results = []
	            while True:
	                result = cursor.fetchall()
	                if cursor.description:
	                    results.append(result)
	                if not cursor.nextset():
	                    break
	            return results
	    
	    def table_exists(self, table_name: str) -> bool:
	        """检查表是否存在"""
	        sql = """
	            SELECT COUNT(*) as count 
	            FROM information_schema.tables 
	            WHERE table_schema = DATABASE() 
	            AND table_name = %s
	        """
	        result = self.query_one(sql, (table_name,))
	        return result['count'] > 0 if result else False
	    
	    def get_table_info(self, table_name: str) -> List[Dict]:
	        """获取表结构信息"""
	        sql = """
	            SELECT column_name, data_type, is_nullable, 
	                   column_default, column_key, extra
	            FROM information_schema.columns 
	            WHERE table_schema = DATABASE() 
	            AND table_name = %s
	            ORDER BY ordinal_position
	        """
	        return self.query_all(sql, (table_name,))
	    
	    def health_check(self) -> bool:
	        """数据库健康检查"""
	        try:
	            result = self.query_one("SELECT 1 as status")
	            return result['status'] == 1 if result else False
	        except Exception as e:
	            logger.error(f"数据库健康检查失败: {e}")
	            return False
	            
	---------------------------------------------------
	
	# usage_examples.py
	from db_utils import DBUtils, DatabaseConfig
	import time
	
	# ============ 初始化配置 ============
	# 方式1: 直接传递参数
	config1 = DatabaseConfig(
	    host='localhost',
	    user='root',
	    password='123456',
	    database='test',
	    port=3306
	)
	
	# 方式2: 从配置文件加载
	config2 = DatabaseConfig(config_file='db_config.json')
	
	# 初始化数据库工具（单例）
	db = DBUtils(config=config1, use_pool=True)
	
	# ============ 基础CRUD操作 ============
	def basic_crud_examples():
	    """基础CRUD示例"""
	    
	    # 1. 查询单条
	    user = db.query_one(
	        "SELECT * FROM users WHERE id = %s", 
	        (1,)
	    )
	    print(f"查询用户: {user}")
	    
	    # 2. 查询多条
	    active_users = db.query_all(
	        "SELECT * FROM users WHERE is_active = %s ORDER BY id DESC", 
	        (1,)
	    )
	    print(f"活跃用户数: {len(active_users)}")
	    
	    # 3. 分页查询
	    page_result = db.query_page(
	        "SELECT * FROM orders WHERE status = %s",
	        ('completed',),
	        page=1,
	        page_size=10
	    )
	    print(f"第1页订单: {len(page_result['data'])}条，共{page_result['total']}条")
	    
	    # 4. 插入数据
	    new_user_id = db.insert('users', {
	        'username': 'test_user',
	        'email': 'test@example.com',
	        'age': 25,
	        'created_at': time.strftime('%Y-%m-%d %H:%M:%S')
	    })
	    print(f"新用户ID: {new_user_id}")
	    
	    # 5. 更新数据
	    affected = db.update(
	        table='users',
	        data={'age': 26, 'updated_at': time.strftime('%Y-%m-%d %H:%M:%S')},
	        where='id = %s',
	        where_params=(new_user_id,)
	    )
	    print(f"更新了{affected}条记录")
	    
	    # 6. 删除数据（软删除示例）
	    deleted = db.update(
	        table='users',
	        data={'is_deleted': 1, 'deleted_at': time.strftime('%Y-%m-%d %H:%M:%S')},
	        where='id = %s AND is_deleted = %s',
	        where_params=(new_user_id, 0)
	    )
	    print(f"软删除了{deleted}条记录")
	
	# ============ 事务操作示例 ============
	def transaction_example():
	    """银行转账事务示例"""
	    
	    try:
	        with db.transaction() as conn:
	            # 在同一个事务中执行多个操作
	            with conn.cursor() as cursor:
	                # 扣除转出账户余额
	                cursor.execute(
	                    "UPDATE accounts SET balance = balance - %s WHERE id = %s AND balance >= %s",
	                    (100.00, 1, 100.00)
	                )
	                if cursor.rowcount == 0:
	                    raise Exception("转出账户余额不足或不存在")
	                
	                # 增加转入账户余额
	                cursor.execute(
	                    "UPDATE accounts SET balance = balance + %s WHERE id = %s",
	                    (100.00, 2)
	                )
	                
	                # 记录交易日志
	                cursor.execute(
	                    """INSERT INTO transactions 
	                       (from_account_id, to_account_id, amount, type) 
	                       VALUES (%s, %s, %s, %s)""",
	                    (1, 2, 100.00, 'transfer')
	                )
	        
	        print("转账成功")
	        
	    except Exception as e:
	        print(f"转账失败: {e}")
	
	# ============ 批量操作示例 ============
	def batch_operations_example():
	    """批量操作示例"""
	    
	    # 批量插入用户
	    users = [
	        {'username': f'batch_user_{i}', 'email': f'user{i}@example.com', 'age': 20+i}
	        for i in range(1, 101)
	    ]
	    
	    affected = db.batch_insert('users', users)
	    print(f"批量插入了{affected}条用户记录")
	    
	    # 批量更新（使用executemany）
	    update_data = [(f'updated_{i}', 20+i, i) for i in range(1, 101)]
	    affected = db.executemany(
	        "UPDATE users SET username = %s, age = %s WHERE id = %s",
	        update_data
	    )
	    print(f"批量更新了{affected}条记录")
	
	# ============ 高级功能示例 ============
	def advanced_features_example():
	    """高级功能示例"""
	    
	    # 1. 检查表是否存在
	    if db.table_exists('users'):
	        print("users表存在")
	        
	        # 2. 获取表结构
	        columns = db.get_table_info('users')
	        print("表结构:")
	        for col in columns:
	            print(f"  {col['column_name']}: {col['data_type']} ({'可空' if col['is_nullable'] == 'YES' else '非空'})")
	    
	    # 3. 健康检查
	    if db.health_check():
	        print("数据库连接正常")
	    else:
	        print("数据库连接异常")
	    
	    # 4. 使用上下文管理器（简化版）
	    with db.get_cursor() as cursor:
	        cursor.execute("SELECT COUNT(*) as count FROM users")
	        result = cursor.fetchone()
	        print(f"用户总数: {result['count']}")
	
	# ============ 数据库监控和统计 ============
	class DatabaseMonitor:
	    """数据库监控类"""
	    
	    def __init__(self, db_utils: DBUtils):
	        self.db = db_utils
	    
	    def get_database_size(self) -> Dict:
	        """获取数据库大小"""
	        sql = """
	            SELECT 
	                table_schema as database_name,
	                ROUND(SUM(data_length + index_length) / 1024 / 1024, 2) as size_mb,
	                ROUND(SUM(data_length) / 1024 / 1024, 2) as data_size_mb,
	                ROUND(SUM(index_length) / 1024 / 1024, 2) as index_size_mb,
	                COUNT(*) as table_count
	            FROM information_schema.tables 
	            WHERE table_schema = DATABASE()
	            GROUP BY table_schema
	        """
	        return self.db.query_one(sql)
	    
	    def get_table_statistics(self) -> List[Dict]:
	        """获取表统计信息"""
	        sql = """
	            SELECT 
	                table_name,
	                table_rows,
	                ROUND((data_length + index_length) / 1024 / 1024, 2) as size_mb,
	                ROUND(data_length / 1024 / 1024, 2) as data_size_mb,
	                ROUND(index_length / 1024 / 1024, 2) as index_size_mb,
	                update_time
	            FROM information_schema.tables 
	            WHERE table_schema = DATABASE()
	            ORDER BY (data_length + index_length) DESC
	        """
	        return self.db.query_all(sql)
	    
	    def get_slow_queries(self, limit: int = 10) -> List[Dict]:
	        """获取慢查询（需要开启慢查询日志）"""
	        sql = """
	            SELECT 
	                query_time,
	                lock_time,
	                rows_sent,
	                rows_examined,
	                db,
	                sql_text
	            FROM mysql.slow_log 
	            ORDER BY query_time DESC
	            LIMIT %s
	        """
	        return self.db.query_all(sql, (limit,))
	
	# ============ 主程序入口 ============
	if __name__ == "__main__":
	    print("=== 数据库工具类演示 ===")
	    
	    # 初始化监控
	    monitor = DatabaseMonitor(db)
	    
	    # 执行示例
	    basic_crud_examples()
	    transaction_example()
	    batch_operations_example()
	    advanced_features_example()
	    
	    # 监控信息
	    db_info = monitor.get_database_size()
	    print(f"\n数据库信息: {db_info}")
	    
    print("\n=== 演示结束 ===")
```
